module.exports = {
    // Bind informations for the HTTP server
    httpBind: "::1",
    httpPort: 8090,

    serverUri: "http://localhost:8090",

    // To generate one, you can use for example: `openssl rand -hex 32`
    apikey: ""
}